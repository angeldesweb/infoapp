import React from 'react';
import Buscar from '../../components/MainRoutes/Buscar';

const BuscarContainer = (props)=>{
    return(
        <Buscar {...props} />
    )
}

export default BuscarContainer;