import React from 'react';
import Crear from '../../components/MainRoutes/Crear/index.jsx';

const CrearContainer = (props)=>{
    return(
        <Crear {...props} />
    )
}

export default CrearContainer;